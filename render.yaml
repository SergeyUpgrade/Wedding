services:
  - type: web
    name: svadebnoe-app
    runtime: python
    buildCommand: |
      pip install poetry
      poetry install --no-dev
    startCommand: |
      poetry run uvicorn svadebnoe.app.main:app --host 0.0.0.0 --port 10000