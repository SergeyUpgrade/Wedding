services:
  - type: web
    name: wedding-poetry
    runtime: python
    buildCommand: |
      python -m pip install --upgrade pip
      pip install poetry
      poetry install --no-root --no-dev
    startCommand: |
      poetry run python -m uvicorn app.main:app --host 0.0.0.0 --port 10000